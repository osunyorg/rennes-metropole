theme: rennes-hugo-theme
_merge: deep
params:
  # RENNES THEME CUSTOM
  custom:
    is_main: true
    external_logo_link:
      active: false

  debug:
    productionUrl: https://metropole.rennes.fr

  home:
    head:
      title: both # both | page | site

  persons:
    index:
      layout: list
    single:
      backlinks: true